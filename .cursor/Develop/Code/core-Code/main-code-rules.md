# Unity 코드 규칙

> 대상: 유니티 게임 개발 팀 | 목적: 일관성 있는 코드 작성

## 세부 규칙 참조
| 작업 상황 | 참조 파일 |
|---|---|
| 코드 포맷·스타일 | [코드스타일.md](./코드스타일.md) |
| 클래스 내부 구조 | [클래스구조.md](./클래스구조.md) |
| 주석 작성 | [주석규칙.md](./주석규칙.md) |
| 코드 패턴·안티패턴 | [코드패턴.md](./코드패턴.md) |

## 핵심 규칙 (항상 적용)
- `[SerializeField]` → `[Tooltip]` 필수
- `public` 메서드·클래스·프로퍼티·변수 → `/// <summary>` 필수
- 매직 넘버 금지 → 상수 또는 `[SerializeField]` 추출
- `GetComponent` → `Awake`에서만 호출
- 인터페이스 → `I` 접두사 필수 (`IDamageable`, `IInteractable`)
- Enum → `E` 접두사 필수 (`EGameState`, `EWeaponType`)
- private 메서드도 public과 동일하게 `/// <summary>` 주석 필수
- 현재 코드베이스에서 참조 0(`rg` 기준)인 코드/구조는 작성 금지

## 운영 원칙
- 처음부터 완벽하게 만들지 않는다
- AI가 규칙을 어긴 경우 → 해당 서브 파일 금지 패턴 목록에 한 줄 추가
